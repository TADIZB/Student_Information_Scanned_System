# Base URL của backend API. Để trống ("") khi dùng Vite proxy ở dev và reverse proxy ở production
# (frontend và backend cùng origin). Chỉ set khi deploy split-origin.
# Ví dụ: VITE_API_BASE=https://api.example.com
VITE_API_BASE=
